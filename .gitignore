# Build artifacts (matches at any depth — the crate lives in codebase/recall)
target/

# Generated release bundles — produced by scripts/release.ps1 and attached
# to GitHub Releases; never committed (see docs/release/RELEASE-CHECKLIST.md)
dist/

# Local databases — never commit memories
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
